export interface UserInputDTO {
    name: string,
    nickname: string,
    email: string,
    password: string,
}

export interface MovieInputDTO {
    title: string,
    description: string,
    duration: number,
    year: number,
}